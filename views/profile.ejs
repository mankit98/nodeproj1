<%- include('partials/header') %>
<%- include('partials/nav') %>
<a class="btn btn-warning  ms-3 text-center fs-2" href="/create-task">Add Book</a>
  <div class="gap-2 content fs-5 p-3  w-75 bg-transparent text-white ">
    <ul class="d-flex justify-content-between w-50 p-2 ">
       <% users.forEach(function(user){ %>
            <li class="text-capitalize list-unstyled fs-2 d-flex justify-content-between"><%=user.username%></li>
            <span><a class="text-decoration-none" href="/delete/<%= user._id %>">❌</a>
                <a class="text-decoration-none" href="/update/<%=user._id %>">✏️</a>
                <a class="text-decoration-none" href="/reset/<%=user._id %>">🔒</a>
            </span> 
      <%  }) %>
        
    </ul>
  </div>

  <div class="cards fs-5 pt-2  border-0 bg-transparent w-100 text-white">
      
    <% posts.forEach(function(post,index){ %>
      <div class="card m-5  bg-transparent d-flex justify-content-center align-items-center flex-column text-white">
        <div class="cont  p-3 ">
            <!-- <p class="fs-3"><%= post.count %></p> -->
          <img width="200"  height="250" src="<%=post.img%>" alt="">
          <h1><%=post.heading%></h1>
          <p><%=post.para %></p>
          <a class="btn btn-primary" href="/update-task/<%= index %>">Update</a>
          <a class="btn btn-danger" href="/delete-task/<%= index %>">Delete</a>
        </div>
        </div>
        <%  }) %>
    </div>
<%- include('partials/footer') %>
